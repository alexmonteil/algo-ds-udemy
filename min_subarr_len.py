# write a function which accepts 2 parameters: an array of positive integers and a positive integer
# the function returns the minimal length of a contiguous subarray of which the sum is greater than or equal 
# to the integer passed to the function. if there isn't one it returns 0


def min_subarr_len(arr, num):

    arrlen = len(arr)
    if arrlen == 0 or num < 0:
        return 0
    
    sum = 0
    templen = 0
    
        
    
        


